# Reset config for wiping runs from selected UTGs.
#
# Defaults to dry-run for safety. Use `--apply` on CLI or set dry_run: false
# after verifying the preview.
dry_run: true

targets:
  - app_name: "AdAway"
    # Supports single value or list.
    utg_number: [1, 2]

  # - app_name: "AntennaPod"
  #   utg_number: "utg-03"
